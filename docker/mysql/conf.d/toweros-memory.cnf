# Tuned for local/small Docker hosts (~6–8 GB RAM). Increase innodb_buffer_pool_size on larger servers.
[mysqld]
innodb_buffer_pool_size = 256M
innodb_log_buffer_size = 8M
max_connections = 80
performance_schema = OFF
table_open_cache = 400
