/**
 * Shared content width for System Core / Dynamic Entities admin lists.
 * AppShell already pads the main area — avoid nested max-w-6xl (too narrow).
 */
export const adminPageShellClass =
  "mx-auto flex w-full max-w-[1600px] flex-col gap-6";
