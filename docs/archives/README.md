# One-off archives (not deployed)

Drop temporary zips/patches here locally if needed. Contents of this folder are **gitignored** (except this README) and are **never** part of AWS images (Docker context is `backend/` + `frontend/` only).
